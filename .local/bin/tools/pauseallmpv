#!/usr/bin/env sh
# this script will pause all running mpv
# require Mpvsockets script

if [[ $(ls -A /tmp/mpvSockets | head -c1 | wc -c) -ne 0 ]]; then

	for i in $(ls /tmp/mpvSockets/*); do
		echo '{ "command": ["set_property", "pause", true] }' | socat - $i;
	done

fi

exit 0
